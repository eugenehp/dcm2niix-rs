[package]
name = "dcm-sys"
description = "Upstream dcm2niix C++ core — parity reference only (not the product path)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
links = "dcm2niix"

[features]
default = ["jpegls", "openjpeg"]
jpegls = []
openjpeg = []

[build-dependencies]
cc = "1"

[dependencies]
libc = "0.2"
